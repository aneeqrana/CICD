git init
git add .

git remote add origin https://github.com/aneeqrana/cicd.git

  git config --global user.email "email.com"
  git config --global user.name "name"

git commit -m 'first commit'

 

git config --global --add safe.directory /var/www/html
git remote -v

git remote set-url origin https://github.com/aneeqrana/cicd.git
git status
git add . 
git status
git config --global user.email devops@xcellorate.com
git config --global user.name Devops
git commit -m "added webfiles"
git branch
git push origin master

git merge master


if you still face errors even after adding the deploy key try this command 

ssh-add ~/.ssh/ansible (path to the private key)

then run the command again you might be asked to approve the key once its done it will be fine 
